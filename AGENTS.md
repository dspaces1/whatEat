# Project Agent Instructions

- Always run `xcodebuild -scheme whatEat -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build` before final response.
- If the build fails, diagnose and fix the issue, then re-run until it passes.
- Mention the exact `xcodebuild` command used in the final response.
